cmake_minimum_required(VERSION 3.10)

set(PROJECT_NAME "account_app")
project(${PROJECT_NAME} LANGUAGES CXX)

# This value is used when generating the bundle identifier for the app.
set(APPLICATION_ID "com.zalooq.account_app")

# This value is used when generating the binary name.
set(BINARY_NAME "account_app")

# ... rest of the file usually follows, but read_file wasn't done on this file so I don't have the full content.
# I must READ the file first before replacing it, otherwise I overwrite it with truncated content.
